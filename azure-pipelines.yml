trigger:
- main

resources:
- repo: self

variables:
  dockerRegistryServiceConnection: DockerHelm
  dockerfilePath: '$(Build.SourcesDirectory)/Dockerfile'
  tag: '$(Build.BuildId)'
  agentName: 'homeAgent'
  BUILD_CONFIGURATION: 'Release'
  PACKAGE_NAME: 'SampleApp'
  PACKAGE_VERSION: '$(Build.BuildNumber)'
  SBOM_NS_BASE: 'http://sbom.local'

stages:
# -----------------------------
# Stage 1: Code Quality
# -----------------------------
- stage: CodeQuality
  displayName: 'Code Quality Checks'
  jobs:
  - job: LintAndFormat
    displayName: 'Run Linters and Formatters'
    pool:
      name: Default
      demands:
        - agent.name -equals $(agentName)
    steps:
    # Restore dependencies
    - script: dotnet restore
      displayName: 'Restore NuGet packages'

    # Check formatting (whitespace/indentation)
    - script: dotnet format whitespace --verify-no-changes
      displayName: 'Check code formatting'
      failOnStderr: true

    # Run analyzers (report warnings, do not fail pipeline)
    - script: dotnet format analyzers --verify-no-changes
      displayName: 'Run code analyzers'
      continueOnError: true
# -----------------------------
# Stage 2: Test
# -----------------------------
- stage: Test
  displayName: 'Run Unit Tests'
  jobs:
  - job: UnitTests
    displayName: 'Unit Tests'
    pool:
      name: Default
    steps:
      # Restore packages
      - script: dotnet restore SampleApp.sln
        displayName: 'Restore NuGet packages'

      # Run unit tests (no --no-build)
      - script: |
          echo "Running unit tests..."
          dotnet test SampleApp.Tests/SampleApp.Tests.csproj \
            --configuration Release \
            --logger "trx;LogFileName=test_results.trx" \
            --results-directory $(System.DefaultWorkingDirectory)/TestResults
        displayName: 'Run Unit Tests'

      # Publish test results
      - task: PublishTestResults@2
        inputs:
          testResultsFiles: '$(System.DefaultWorkingDirectory)/TestResults/*.trx'
          testRunTitle: 'Unit Tests'
          mergeTestResults: true
          failTaskOnFailedTests: true

# -----------------------------
# Stage 3: Build & Docker
# -----------------------------
- stage: Build
  displayName: 'Build, Scan & SBOM'
  jobs:
  - job: Build
    displayName: 'Build & SBOM'
    pool:
      name: Default
      demands: 
        - agent.name -equals $(agentName)
        - java
    steps:
    # SonarQube preparation
    - task: SonarQubePrepare@7
      displayName: 'Prepare SonarQube Analysis'
      inputs:
        SonarQube: 'SonarQubeDockerHelm'
        scannerMode: 'dotnet'
        projectKey: 'HOME_DockerHelm_0649997a-2597-4daa-bfa7-7214baa79d52'
        projectName: 'DockerHelm'
        extraProperties: |
          sonar.host.url=http://192.168.1.2:9000
          sonar.login=$(SONARQUBE_TOKEN)

    # Restore NuGet packages
    - script: dotnet restore
      displayName: 'Restore NuGet packages'

    # Vulnerability scan for .NET packages
    - script: dotnet list package --vulnerable
      displayName: 'Dependency Vulnerability Scan'

    # Build solution
    - script: dotnet build
      displayName: 'Build Solution'

     # Install sbom-tool globally
    - script: |
        dotnet tool install --global Microsoft.Sbom.DotNetTool --version 4.* || dotnet tool update --global Microsoft.Sbom.DotNetTool --version 4.*
        export PATH="$PATH:$HOME/.dotnet/tools"

        DROP_DIR=$(Build.SourcesDirectory)/SampleApp/bin/$(BUILD_CONFIGURATION)/net7.0/publish
        mkdir -p "$DROP_DIR"

        dotnet publish SampleApp/SampleApp.csproj -c $(BUILD_CONFIGURATION) -o "$DROP_DIR"

        sbom-tool generate \
          -b "$DROP_DIR" \
          -pn $(PACKAGE_NAME) \
          -pv $(PACKAGE_VERSION) \
          -nsb $(SBOM_NS_BASE)

        echo "✅ SBOM generated at $DROP_DIR/_manifest"
      displayName: 'Publish app & generate SBOM'

    - publish: $(Build.SourcesDirectory)/SampleApp/bin/Release/net7.0/publish/_manifest
      artifact: dotnet-sbom
      displayName: 'Publish SBOM artifact'

    # Install CycloneDX .NET tool locally
    - script: |
        cd $(Build.SourcesDirectory)
        dotnet new tool-manifest --force
        dotnet tool install --local CycloneDX --version 2.*
        dotnet tool restore
      displayName: 'Install CycloneDX .NET tool'

    # Publish app
    - script: |
        DROP_DIR=$(Build.SourcesDirectory)/SampleApp/bin/$(BUILD_CONFIGURATION)/net7.0/publish
        mkdir -p "$DROP_DIR"
        dotnet publish SampleApp/SampleApp.csproj -c $(BUILD_CONFIGURATION) -o "$DROP_DIR"
      displayName: 'Publish .NET app'

    # Generate CycloneDX SBOM
    - script: |
        dotnet tool run cyclonedx -p SampleApp/SampleApp.csproj -o $(Build.ArtifactStagingDirectory)/bom.json --format json
      displayName: 'Generate CycloneDX SBOM'

    # Publish SBOM as artifact
    - publish: $(Build.ArtifactStagingDirectory)/bom.json
      artifact: dotnet-sbom
      displayName: 'Publish SBOM artifact'

    # Build & push Docker image
    - task: Docker@2
      displayName: 'Build and Push Docker Image'
      inputs:
        command: buildAndPush
        repository: $(imageRepository)
        dockerfile: $(dockerfilePath)
        containerRegistry: $(dockerRegistryServiceConnection)
        tags: |
          $(tag)

    # Run SonarQube analysis
    - task: SonarQubeAnalyze@7
      displayName: 'Run SonarQube Analysis'

    # Publish SonarQube Quality Gate
    - task: SonarQubePublish@7
      displayName: 'Publish SonarQube Quality Gate'
      inputs:
        pollingTimeoutSec: '300'
# -----------------------------
# Stage 4: Helm Deployment
# -----------------------------
- stage: Deploy
  displayName: 'Deploy to Production'
  dependsOn: Build
  jobs:
  - deployment: HelmDeploy
    displayName: 'Helm Deploy'
    environment: 'production'
    pool:
      name: Default
    strategy:
      runOnce:
        deploy:
          steps:
          - task: HelmInstaller@1
            displayName: 'Install Helm'
            inputs:
              helmVersionToInstall: 'latest'

          - script: |
              echo "Using Kubernetes context: $(KUBE_CONTEXT)"
              kubectl config use-context $(KUBE_CONTEXT)
              helm upgrade --install sampleapp ./charts/sampleapp \
                --namespace mynamespace \
                --set image.repository=dockerhelm.azurecr.io/sampleapp \
                --set image.tag=$(Build.BuildId)
            displayName: 'Deploy Helm Chart'